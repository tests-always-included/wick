#!/bin/bash

wick-get-argument() {
    local ARGS INDEX TARGET

    TARGET=$1
    INDEX=$2
    shift 2
    wick-get-arguments ARGS "$@"
    local "$TARGET" && wick-indirect "$TARGET" "${ARGS[$INDEX]}"
}
